# Extra apt packages to install on top of the base image.
# One package per line.  Lines starting with # and blank lines are ignored.
# Only this layer re-runs when you add a package — the base layers stay cached.
#
# Examples:
#   tree
#   bat
#   httpie
#   ncdu
#   nmap


jq
htop
btop
ncdu
tree
ranger
